[phases.setup]
nixPkgs = ["binutils", "gcc", "rust-bin.stable.latest.default"]

[phases.install]
cmds = ["cargo fetch"]

[phases.build]
env = { SQLX_OFFLINE = "true" }

[variables]
SQLX_OFFLINE = "true"
RUST_LOG = "trace"
