[phases.install]
cmds = ["cargo fetch"]

[phases.build]
env = { SQLX_OFFLINE = "true" }
cmds = ["cargo build --release"]

[phases.start]
cmd = "./target/release/superchain-indexer"

[variables]
SQLX_OFFLINE = "true"
RUST_LOG = "trace"
